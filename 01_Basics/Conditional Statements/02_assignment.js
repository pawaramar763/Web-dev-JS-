let color = "green";

if (color === "red") {
    console.log(`Stop ! it's ${color} signal`);
}

if (color === "yellow") {
    console.log(`Go slow ! it's ${color} signal`);
}

if (color === "green") {
    console.log(`Go  ! it's ${color} signal`);
}